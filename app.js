const students = require('./students.js')
const { instructors, locations, Student } = require('./staff.js')

// students.forEach(student => console.log(student));
console.log(locations);

const x = new Student('Paul', 22);

console.log(x);
